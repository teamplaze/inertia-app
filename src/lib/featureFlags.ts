// Slot limits are disabled for the new tier sales model (unlimited
// availability during fixed sales windows). Flip to true to
// re-enable for projects that want limited-quantity tiers.
export const SHOW_SLOT_LIMITS = false;
